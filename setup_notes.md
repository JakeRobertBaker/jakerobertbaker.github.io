# Notes from Fresh Setup

## Quarto
Install quarto via CLI on the quarto website instructions. Then install Quarto VSCode Extension.

## TEX
For the TIKZ plots.
`sudo apt install texlive`
Then
`sudo apt install texlive-full`